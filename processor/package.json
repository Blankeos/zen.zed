{
  "name": "processor",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "generate": "bun run src/main.ts",
    "dev": "nodemon --watch src --ext ts,js,json,json5 --exec \"bun run generate\""
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^24.8.1",
    "nodemon": "^3.1.10"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "color": "^5.0.2",
    "hexa": "^1.2.0",
    "json5": "^2.2.3"
  }
}
